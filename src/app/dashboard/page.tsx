// This file satisfies the route for the exact "/dashboard" path itself.
// The visible content comes entirely from the @submissions, @analytics,
// and @settings parallel slots rendered by the layout.
export default function DashboardIndexPage() {
  return null;
}
